<div class="row">
    <div class="col-md-4">
        <table class='table'>
            <thead>
                <tr><th>反馈列表</th></tr>
            </thead>
            <tbody>
            {{#each controller}}
                <tr><td>{{#link-to 'detail' this}}{{title}}{{/link-to}}</td></tr>
            {{/each}}
            </tbody>
        </table>
    </div>
    <div class="col-md-8">
        {{outlet}}
    </div>
</div>